<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Fee_Rate_details_can_not_be_changed</fullName>
    <active>true</active>
    <description>Hourly Rate, Start Date, Band &amp; Weighting fields are prevented from being changed.</description>
    <errorConditionFormula>AND(NOT(ISNEW()),
    OR(ISCHANGED(Geographical_Weighting__c),
       ISCHANGED(Hourly_Rate__c),
       ISCHANGED(Practitioner_Band__c),
       ISCHANGED(Start_Date__c)),
    NOT($Permission.Override_Practitioner_Fee_Rate_Validation)
)</errorConditionFormula>
    <errorMessage>The only field you can change on a Practitioner Fee Rate Record is the end date. If you need to change any other field please apply an end date, equal to the start date, to this Record and then create a new Record with the correct details.</errorMessage>
</ValidationRule>
