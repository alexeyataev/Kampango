<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>PractitionerBand_details_cant_be_changed</fullName>
    <active>true</active>
    <description>Band, Start Date fields are prevented from being changed.</description>
    <errorConditionFormula>AND(NOT(ISNEW()),
    OR(ISCHANGED(Band__c),
       ISCHANGED(Start_Date__c)),
    NOT($Permission.Override_Practitioner_Band_Validation)
)</errorConditionFormula>
    <errorMessage>The only field you can change on a Practitioner Band Record is the end date. If you need to change any other field please apply an end date, equal to the start date, to this Record and then create a new Record with the correct details.</errorMessage>
</ValidationRule>
