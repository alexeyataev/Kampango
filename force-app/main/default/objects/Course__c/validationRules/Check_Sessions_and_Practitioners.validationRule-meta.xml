<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Check_Sessions_and_Practitioners</fullName>
    <active>true</active>
    <errorConditionFormula>AND(
    OR(ISNEW(), 
       ISCHANGED(Status__c)),
    ISPICKVAL(Status__c, &quot;Final&quot;), 
    OR(Count_of_Unconfirmed_Non_Reunion_Session__c &gt; 0, 
       Count_of_Sessions_Without_Practitioner__c &gt; 0, 
       Number_of_Sessions__c &lt; 1), 
    NOT($Permission.Override_Course_Validation))</errorConditionFormula>
    <errorMessage>Please ensure that all sessions have a practitioner assigned and all non-reunion sessions are confirmed.</errorMessage>
</ValidationRule>
