<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Check_Session_Fee_Rates</fullName>
    <active>true</active>
    <errorConditionFormula>AND(OR(ISNEW(), ISCHANGED(Status__c)),
    ISPICKVAL(Status__c, &quot;Final&quot;), 
    Count_of_Sessions_Without_Fee_Rates__c &lt;&gt; 0,
    NOT($Permission.Override_Course_Validation)
)</errorConditionFormula>
    <errorMessage>One or more sessions is missing a practitioner fee rate. You may need to work with a system administrator to ensure that Practitioner Band, Branch Weighting and Fee Rate data all exists, covering the dates of your sessions.</errorMessage>
</ValidationRule>
